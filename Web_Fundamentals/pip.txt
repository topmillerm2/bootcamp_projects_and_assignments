1. “pip install django” didn’t work and returned: “-bash: pip: command not”-researched and found using “pip3 install Django” might make it work.  This worked and installed Django2. used “pip3 list”  result:-DEPRECATION: The default format will switch to columns in the future. You can use --format=(legacy|columns) (or define a format=(legacy|columns) in your pip.conf under the [list] section) to disable this warning.Researched that this isn’t necessarily an error but just a warning of a change to come3. “pip3 install Django” again: result: indication that Django is already installed on comp4. “pip3 freeze” result: freeze uses “==” and list uses “()”running “pip3 > requirements.txt” created a new file on my desktop5. “pip3 uninstall Django” deleted a bunch of the files for Django6. “pip3 show Django” returned nothing and came back to a new command line7. “pip3 search Flask” returned a ton of files all containing at least one “Flask” in the title or description 